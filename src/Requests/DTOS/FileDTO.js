class FileDTO {
    constructor(path, filename,type,tags,metadata) {
      this.path = path;
      this.filename = filename;
      this.type = type;
      this.tags = tags;
      this.metadata = metadata;
    }
  }